# --- Agent orchestration ---
crewai>=0.100.0

# --- LLM clients (both free options; only one is needed at runtime) ---
# CrewAI talks to both via its built-in LLM class (litellm under the hood),
# so no separate langchain client packages are required.
#   - Groq free tier: set LLM_PROVIDER=groq + GROQ_API_KEY
#   - Local Ollama:   set LLM_PROVIDER=ollama + OLLAMA_BASE_URL

# --- Embeddings + vector store (all local, no paid API) ---
# ChromaDB ships an ONNX build of all-MiniLM-L6-v2 as its default embedding
# function (onnxruntime, ~50MB) — same model the PRD specifies, but without a
# ~1GB PyTorch/sentence-transformers dependency. See PRD.md Section 3 note.
chromadb>=0.5.0

# --- Market data + news ---
yfinance>=0.2.43
pandas>=2.2.0
numpy>=1.26.0
feedparser>=6.0.11          # Google News RSS fallback when yfinance .news breaks
requests>=2.31.0

# --- API + frontend ---
fastapi>=0.115.0
uvicorn>=0.30.0
streamlit>=1.37.0

# --- Utilities ---
python-dotenv>=1.0.0
pydantic>=2.7.0
loguru>=0.7.2

# NOTE: Do not add openai / langchain-openai / pinecone-client / weaviate-client
# / sentence-transformers+torch unless deliberately swapping in a heavier or
# paid tier — this project is scoped to run on free/open-source infrastructure
# inside <1GB RAM. See PRD.md Section 3.
